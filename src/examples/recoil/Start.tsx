import React from 'react';
import { Box } from '@chakra-ui/react';

export default function Start() {
  return (
    <Box mt={8}>
      <div>Atomic state management with Recoil and Jotai</div>
      <iframe title="recoil react" src="https://recoiljs.org" width={1700} height={900} />
    </Box>
  )
}